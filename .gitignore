# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
build/
*.moved-aside
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata/

# Swift Package Manager
.build/
Packages/

# CocoaPods
Pods/

# macOS
.DS_Store
*.swp
*~

# Dev credentials (never commit)
dev-config.json

# Vendor (root-level cloned reference repos). Anchored to repo root so it
# doesn't also swallow chat4000/Vendor/ (the vendored Matrix crypto bindings,
# which we DO track as text — only the 505MB xcframework binary is excluded
# below and rebuilt locally per the crypto build steps).
/vendor/

# Rust
relay/target/

# Figma screens (web prototype, not part of iOS build)
figma-screens/node_modules/
figma-screens/dist/

# Claude Code session state
.claude/
.matrix-ffi-ref/
chat4000/Vendor/MatrixSDKCrypto/MatrixSDKCryptoFFI.xcframework/
